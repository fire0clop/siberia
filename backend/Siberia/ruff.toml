target-version = "py312"

[lint]
select = ["E4", "E7", "E9", "F"]

[lint.per-file-ignores]
# models/__init__.py намеренно ре-экспортирует все модели: это регистрирует их
# в SQLAlchemy metadata для alembic и разрешает relationship() по имени класса.
"models/__init__.py" = ["F401"]
